# OpenRouter (OpenAI-compatible Chat Completions)
# Use the raw API key only; do not prefix with "Bearer".
OPENROUTER_API_KEY=sk-or-v1-your-real-openrouter-key

# Use the API base root only (not /chat/completions).
OPENROUTER_API_BASE=https://openrouter.ai/api/v1

# Model route used by the backend LLM extraction step.
LLM_MODEL=nvidia/nemotron-3-super-120b-a12bdeepinfra/bf16

# Optional OpenRouter metadata headers.
OPENROUTER_SITE_URL=https://your-site.example
OPENROUTER_SITE_NAME=snv-PII-redact

# Optional timeout for the upstream model call.
LLM_REQUEST_TIMEOUT_SECONDS=60

# Backend debug logging (set to 1 to print detailed span/fuzzy diagnostics).
BACKEND_DEBUG_BLOCKS=0

# LLM span localization guardrails.
LLM_MAX_LOCALIZED_ENTITY_TOKENS=14
LLM_MAX_LOCALIZED_ENTITY_CHARS=140

# Fuzzy localization controls for quote-to-span matching.
FUZZY_MIN_TOKEN_PADDING=1
FUZZY_MAX_TOKEN_PADDING=2
FUZZY_LENGTH_PENALTY_PER_EXTRA_TOKEN=2.0
FUZZY_LENGTH_PENALTY_CAP=14.0

# Debug threshold for logging large offset->bbox lookups.
BBOX_DEBUG_MIN_SPAN=80
